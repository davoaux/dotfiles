[module/powermenu]
type = custom/script
exec = echo "   "
;exec = echo " "
interval = 300
click-left = ~/scripts/powermenu &
format-foreground = ${colors.minimal-fg-alt}

[module/launcher]
type = custom/script
exec = echo "   "
;exec = echo "   "
interval = 300
click-left = rofi -show drun
format-foreground = ${colors.minimal-fg-alt}

[module/screenshot]
type = custom/script
exec = echo "   "
interval = 300
click-left = ~/scripts/prtscr &
format-foreground = ${colors.minimal-fg-alt}

[module/spotify]
type = custom/script
interval = 1
;format-prefix = "  "
;format-prefix-foreground = #1DB964
;format-prefix = " "
format = <label>
format-foreground = ${colors.minimal-fg-alt}
exec = python /home/parelkobra/.config/polybar/custom_modules/spotify_status.py -f '{song} - {artist}    '
;format-underline = #1db954

; Packages that can be upgraded
[module/pkgs]
type = custom/script
exec = /home/parelkobra/.config/polybar/custom_modules/pkgs.sh
interval = 1000
format-foreground = ${colors.minimal-fg-alt}

[module/xwindow]
type = internal/xwindow
;label = %title:0:30:...%
format = <label>
label = %title%
label-padding = 1

[module/i3]
type = internal/i3
format = <label-state> <label-mode>
index-sort = true
wrapping-scroll = false

; Only show workspaces on the same output as the bar
;pin-workspaces = true

label-mode-padding = 2
label-mode-foreground = #000
label-mode-background = ${colors.primary}

; focused = Active workspace on focused monitor
label-focused = %index%
;label-focused-background = ${colors.bg-alt}
;label-focused-background = #333
label-focused-background = ${colors.minimal-bg}
label-focused-foreground = ${colors.minimal-fg-alt}
label-focused-underline= ${colors.primary}
label-focused-padding = 2

; unfocused = Inactive workspace on any monitor
label-unfocused = %index%
label-unfocused-padding = 2

; visible = Active workspace on unfocused monitor
label-visible = %index%
label-visible-background = ${self.label-focused-background}
label-visible-underline = ${self.label-focused-underline}
label-visible-padding = ${self.label-focused-padding}

; urgent = Workspace with urgency hint set
label-urgent = %index%
label-urgent-background = ${colors.alert}
label-urgent-padding = 2

; Separator in between workspaces
; label-separator = |

[module/xbacklight]
type = internal/xbacklight

format = <label> <bar>
label = BL

bar-width = 10
bar-indicator = |
bar-indicator-foreground = #fff
bar-indicator-font = 2
bar-fill = ─
bar-fill-font = 2
bar-fill-foreground = #9f78e1
bar-empty = ─
bar-empty-font = 2
bar-empty-foreground = ${colors.fg-alt}

[module/backlight-acpi]
inherit = module/xbacklight
type = internal/backlight
card = intel_backlight

[module/cpu]
type = internal/cpu
interval = 2
;format-prefix = "  "
;format-prefix = " "
format-prefix-background= ${colors.bg}
format-underline = #f90000
label = "%percentage:2%% "
;label-background = ${colors.bg}

[module/memory]
type = internal/memory
interval = 2
;format-prefix = "  "
;format-prefix = "  "
format-prefix-foreground = ${colors.green}
format-underline = #4bffdc
;label = "%percentage_used%% "
label = " %gb_used% "

[module/eth]
type = internal/network
interface = enp7s0
interval = 3.0

format-connected-underline = #55aa55
;format-connected-prefix = " "
format-connected-prefix-foreground = ${colors.fg-alt}
label-connected = " %local_ip% "
label-connected-background = ${colors.blue}

format-disconnected =
;format-disconnected = <label-disconnected>
;format-disconnected-underline = ${self.format-connected-underline}
;label-disconnected = %ifname% disconnected
;label-disconnected-foreground = ${colors.fg-alt}

[module/minimal_date]
type = internal/date
interval = 5

date =
date-alt =

time = "%a, %B %d %H:%M"
time-alt = "%a, %B %d %H:%M"

label = %date% %time%
label-foreground = ${colors.minimal-fg-alt}

[module/date]
type = internal/date
interval = 5

date =
date-alt = " %A %d %B %Y"

;time = "%A %d %B, %H:%M "
time = "%A %d, %H:%M "
;time = " %H:%M "
;time-alt = "  %H:%M:%S "
time-alt = " %H:%M:%S "

;format-prefix = 
format-prefix =" "
format-prefix-foreground = ${colors.blue}
;format-underline = #0a6cf5

label = %date% %time%
;label-background = ${colors.blue}
label-background = ${colors.bg}

[module/pulseaudio]
type = internal/pulseaudio

format-volume = <ramp-volume><label-volume>
label-volume = "%percentage%%"
;label-volume-foreground = ${root.foreground}
label-volume-foreground = ${colors.yellow-light}
;label-volume-background = ${colors.red}

format-muted = <label-muted>
;label-muted = "  muted "
label-muted = "muted"
;label-muted-background = ${colors.red}
label-muted-foreground = ${colors.yellow-light}

;ramp-volume-0 = "  "
ramp-volume-0 = " "

[module/temperature]
type = internal/temperature
thermal-zone = 0
warn-temperature = 60

format = "<ramp><label>"
;format = <label>
format-underline = #f50a4d
format-warn = <ramp> <label-warn>
format-warn-underline = ${self.format-underline}

label = " %temperature-c% "
label-background = ${colors.blue}
label-warn = %temperature-c%
label-warn-foreground = ${colors.secondary}

ramp-0 = " "
ramp-1 = " "
ramp-2 = " "
;ramp-0 = " "
;ramp-1 = "   "
;ramp-2 = " "
ramp-foreground = ${colors.fg}
ramp-background = ${colors.blue}

[settings]
screenchange-reload = true
;compositing-background = xor
;compositing-background = screen
;compositing-foreground = source
;compositing-border = over
;pseudo-transparency = false 

[global/wm]
margin-top = 5
margin-bottom = 5

; vim:ft=dosini
